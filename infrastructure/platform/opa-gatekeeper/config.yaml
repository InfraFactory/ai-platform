apiVersion: config.gatekeeper.sh/v1alpha1
kind: Config
metadata:
  name: config
  namespace: gatekeeper-system
spec:
  match:
    - excludedNamespaces:
        - flux-system
        - gatekeeper-system
        - kube-system
        - monitoring
      processes:
        - "*"
